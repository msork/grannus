[package]
name = "grannus-host"
description = "Grannus host daemon and command-line interface"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
grannus-core = { path = "../grannus-core" }
grannus-platform = { path = "../grannus-platform" }
grannus-protocol = { path = "../grannus-protocol" }
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.6"

[target.'cfg(unix)'.dependencies]
signal-hook = "0.3.18"

[lints]
workspace = true
