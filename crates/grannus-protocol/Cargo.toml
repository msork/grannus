[package]
name = "grannus-protocol"
description = "Versioned wire protocol for Grannus and Sirona"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
grannus-core = { path = "../grannus-core" }

[lints]
workspace = true
